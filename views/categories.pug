extends layout

block content
  .container
    h1 #{title}
    ul.list-group
      each category in categories
        li.list-group-item
          a(href='/articles/category/'+category._id)=category.title
